if you want to show a selected image that you wnat to uplaod and preview it you can use teh code -->

const file=e.target.file[0];
const reader=new FileReader();

reader.readAsDataURL(file);
reader.onload=async()=>{
    //const into base 64
    const base64image=reader.result;
    //update the image holding state
    await updateprofile({image:base64image})
}