if you want to show a selected image that you wnat to uplaod and preview it you can use teh code -->

const file=e.target.file[0];
const reader=new FileReader();

reader.readAsDataURL(file);
reader.onload=async()=>{
    //const into base 64
    const base64image=reader.result;
    //update the image holding state
    await updateprofile({image:base64image})
}

//We attach Socket.IO to the HTTP server (not directly to Express) because:
// Socket.IO needs low-level access to the real network server to manage WebSocket connections, upgrades, and long-lived connections — which Express alone cannot do.
// Express is just a request–response handler.
// Socket.IO needs the actual server that listens on a port.